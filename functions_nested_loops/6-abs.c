#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer.
 *
 * @i: value of integer.
 *
 * Return: 0 is a success.
 */
int _abs(int i)
{
	if (i < 0)
	return (-i);
	{
	if (i > 0)
	return (i);
	}
	return (0);
}
